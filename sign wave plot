****sign wave plot***

from matplotlib import pyplot as plt
impotr numpy as np
import math 
#needed for defination of pi
x=np.arange(0,math.pi*2,0.05)
y=np.sin(x)
#print(x)
#print(y)
plt.plot(x,y)
plt.xlabel("angel")
plt.ylabel('sine')
plt.title('sine wave')
plt.show()
